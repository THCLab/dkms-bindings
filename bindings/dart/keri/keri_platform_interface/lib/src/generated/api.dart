// This file is automatically generated, so please do not edit it.
// @generated by `flutter_rust_bridge`@ 2.12.0.

// ignore_for_file: invalid_use_of_internal_member, unused_import, unnecessary_import

import 'frb_generated.dart';
import 'package:flutter_rust_bridge/flutter_rust_bridge_for_generated.dart';
import 'types.dart';

// These functions are ignored because they are not marked as `pub`: `algorithm_for_label`, `alias_from_label`, `basic_prefix_for`, `block_on_dart`, `default_algorithm`, `get_factory`, `get_signer`, `key_state_path`, `load_key_state`, `open_store`, `parse_algorithm`, `resolve_location_scheme`, `save_key_state`, `signer_algorithm`
// These types are ignored because they are neither used by any `pub` functions nor (for structs and enums) marked `#[frb(unignore)]`: `KeyState`
// These function are ignored because they are on traits that is not defined in current crate (put an empty `#[frb]` on it to unignore): `clone`, `fmt`

// Rust type: RustOpaqueMoi<flutter_rust_bridge::for_generated::RustAutoOpaqueInner<KeriMobileSdk>>
abstract class KeriMobileSdk implements RustOpaqueInterface {
  Future<FfiCredentialStatus> checkCredential(
      {required String alias,
      required String registryId,
      required String credentialSaid});

  /// Create a new KERI identifier. Mints the current and next signing keys
  /// in the host key provider (one `createKey` callback each), commits to
  /// the next-key, and persists the alias-to-label mapping so subsequent
  /// signs and rotations pick up the right labels.
  Future<String> createIdentifier(
      {required String alias, required FfiIdentifierConfig config});

  Future<FfiCredentialStatus> getCredentialStatus(
      {required String alias, required String credentialSaid});

  Future<String> inceptRegistry({required String alias});

  Future<void> issueCredential(
      {required String alias, required String credentialSaid});

  Future<List<String>> listAliases();

  Future<String> loadIdentifier({required String alias});

  // HINT: Make it `#[frb(sync)]` to let it become the default constructor of Dart class.
  static Future<KeriMobileSdk> newInstance({required String dbPath}) =>
      RustLib.instance.api.crateApiKeriMobileSdkNew(dbPath: dbPath);

  /// Register the host-side key provider. Each callback is a Dart async
  /// function: errors should be thrown as Dart exceptions, which propagate
  /// here as Rust errors via `flutter_rust_bridge`'s `DartFnFuture` adapter.
  Future<void> registerKeyProvider(
      {required FutureOr<Uint8List> Function(String, String) createKey,
      required FutureOr<Uint8List> Function(String) openKey,
      required FutureOr<Uint8List> Function(String, Uint8List) sign,
      required FutureOr<void> Function(String) deleteKey,
      required FutureOr<List<String>> Function() listKeys});

  Future<void> revokeCredential(
      {required String alias, required String credentialSaid});

  /// Rotate the signing keys for [alias].
  ///
  /// Mechanics: the next-key that was committed in the previous event is
  /// revealed and becomes the new current signer; we mint a fresh
  /// next-next-key via the host callback and commit to it. On success the
  /// previous current key is wiped from the host keystore.
  Future<void> rotateKeys(
      {required String alias, required FfiRotationConfig config});

  /// Return the saved AID, the raw KEL (CESR bytes as String, lossy UTF-8),
  /// and whether the controller's storage currently has a key state record
  /// for that AID. Diagnostic helper — also useful for "show KEL" UI.
  Future<String> showKel({required String alias});

  Future<FfiSignedEnvelope> sign(
      {required String alias, required List<int> data});

  Future<FfiVerifiedPayload> verify(
      {required String alias, required List<int> cesr});

  /// Read the bytes the host currently has for `<alias>_v2`, reconstruct
  /// the `BasicPrefix` that should match the inception's next-key commitment,
  /// and compare its Blake3 digest to the digest persisted in the KEL.
  /// Returns a multi-line string so a human can eyeball every field.
  Future<String> verifyNextBinding({required String alias});

  /// Drop the cached `KeriStore` (releasing the redb flock) and recursively
  /// delete `db_path`. Idempotent. The Android-side keystore is wiped
  /// separately by the host, since this struct doesn't own those secrets.
  Future<void> wipe();
}
