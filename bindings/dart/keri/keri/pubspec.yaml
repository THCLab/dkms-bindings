name: keri
description: Dart client for managing KERI based Identifiers on Android system.
version: 1.1.5
homepage: https://github.com/THCLab/keri-bindings/tree/master/bindings/dart/keri

environment:
  sdk: ">=2.18.0 <3.0.0"
  flutter: ">=3.0.0"

dependencies:
  flutter:
    sdk: flutter
  keri_android: ^0.0.6
  keri_ios: ^0.0.1
  keri_macos: ^0.0.1
  keri_platform_interface: ^0.0.4
  keri_windows: ^0.0.4
  plugin_platform_interface: ^2.1.3

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.0
  ffigen: ^6.1.2
  build_runner: ^2.2.1


flutter:
  plugin:
    platforms:
      android:
        default_package: keri_android
      ios:
        default_package: keri_ios
      windows:
        default_package: keri_windows
      macos:
        default_package: keri_macos
